syntax = "proto3";

service RaftManager {
  rpc SendActive(SendActiveRequest) returns (SendActiveResponse);
}

message SendActiveRequest {
  int32 nodeId = 1;
  bool active = 2;
}

message SendActiveResponse { bool success = 1; }